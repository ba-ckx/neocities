{
  "name": "neocities",
  "version": "0.0.1",
  "private": true,
  "description": "Neocities site with Eleventy blog outputting to /public",
  "scripts": {
    "build": "npx @11ty/eleventy",
    "dev": "npx @11ty/eleventy --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "luxon": "^3.5.0"
  }
}
