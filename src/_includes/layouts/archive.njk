---
pagination:
  data: collections.postsByMonth
  size: 1
  alias: archiveGroup
permalink: "blog/archive/{{ archiveGroup.month }}/index.html"
---
{% include "header.njk" %}
<div class="content-wrapper">
  <main class="archive-page">
    <h1>Archive: {{ archiveGroup.month | monthYear }}</h1>
    <ul class="post-list">
      {% for post in archiveGroup.posts %}
        <li>
          <a href="{{ post.url }}">{{ post.data.title }}</a> 
          <small>({{ post.date | dateReadable }})</small>
        </li>
      {% endfor %}
    </ul>
    <p><a href="/blog/">&#8592; Back to all posts</a></p>
  </main>
  {% include "sidebar.njk" %}
</div>
{% include "footer.njk" %}
