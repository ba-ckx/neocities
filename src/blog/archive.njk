---
pagination:
  data: collections.postsByMonth
  size: 1
  alias: monthGroup
permalink: "blog/archive/{{ monthGroup.month }}/index.html"
title: "Archive"
---
{% include "header.njk" %}
<div class="content-wrapper">
  <main class="archive-page">
    <h1>Archive · {{ monthGroup.month | monthYear }}</h1>

    {% if monthGroup.posts and monthGroup.posts.length %}
      <ul class="post-list">
        {% for post in monthGroup.posts %}
          <li>
            <a href="{{ post.url }}">{{ post.data.title }}</a>
            <small> · {{ post.date | dateReadable }}</small>
          </li>
        {% endfor %}
      </ul>
    {% else %}
      <p>No posts for this month.</p>
    {% endif %}

    <p><a href="/blog/">← Back to all posts</a></p>
  </main>

  {% include "sidebar.njk" %}
</div>
{% include "footer.njk" %}
